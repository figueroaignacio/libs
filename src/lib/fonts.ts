import { Geist_Mono, Manrope } from "next/font/google";

export const fontSans = Manrope({
  variable: "--font-sanst-sans",
  subsets: ["latin"],
});

export const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});
